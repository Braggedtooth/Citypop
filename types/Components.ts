export type CountryResultsProps = {
  results: { name: string; population: number; countryName: string }
  details: any
}
export type CityResultsProps = {
  results: { name: string; population: number }
}
